# Programa para calcular área do paralelogramo

base = float(input("insira o comprimento da base: "))
altura = float(input("insira a altura: "))

area = base * altura

print("comprimento da base: ", base, "altura: ", altura, "area: ", area)